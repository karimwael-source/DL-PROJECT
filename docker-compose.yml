version: '3.8'

services:
  dl-project:
    build: .
    container_name: dl-project-app
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./static/outputs:/app/static/outputs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
